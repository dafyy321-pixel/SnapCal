# SnapCal 审查修复记录 · 2026-09-08

原审查基线：`b5e8cd8`。16 项缺陷和 4 项优化建议已处理。每个独立功能修复通过相关测试后提交；最终检查发现的问题另行修复、验证和提交。

| 编号 | 修复结果 | 主要提交 |
|---|---|---|
| 01 | 日期表单绑定已加载日期，拒绝过期响应，加载失败时禁用写入并支持重试 | `6854144`、`fdd7e38` |
| 02 | AI 输入先排除已知过敏食材，主方案和替代方案返回后再校验安全 ID 集合 | `510d3d4` |
| 03 | 草稿转换为餐食 DTO；保存错误有反馈，关联失败保留餐食 ID，关联接口支持幂等重试 | `818f81a`、`5c5efa8` |
| 04 | 多值配置支持顿号、中英文逗号，加载后直接保存保持列表内容 | `e7c422a` |
| 05 | 动作使用稳定的编辑标识，改名、排序和删除其他动作不重建输入框 | `68cf28e` |
| 06 | 保存、删除开始清理旧提示；失败保留输入、显示错误并允许重试 | `6854144` |
| 07 | 将可访问名称传递给真正的 slider Thumb，支持多个 Thumb 分别命名 | `48690dd` |
| 08 | 餐食、状态、训练和配置变更在数据库事务内使有效行动卡失效 | `e0fe151`、`0c7f92a` |
| 09 | 每周训练目标按已完成训练日期去重，快照和说明同步使用天数 | `241a615` |
| 10 | 有氧距离只计入已完成训练中的已完成组 | `82e70be` |
| 11 | 使用统一配置时区的下一日起点作为排他到期边界，覆盖 UTC 和夏令时 | `e83a58b` |
| 12 | 新迁移将唯一约束恢复为每天一张有效卡，并验证旧 v3 数据库升级 | `0c7f92a` |
| 13 | 写接口校验完整 Origin 和媒体类型；使用实际 Host 处理 Next 回环地址规范化 | `d19982b`、`7ecf317` |
| 14 | 所有 AI 任务共用每小时 20 次发送额度、两个并发上限和进行中任务合并；重试计入额度 | `90dd30e` |
| 15 | 每次外发前重新读取同意状态；撤回后不发送自动重试 | `fb65b99` |
| 16 | 按 node:sqlite 的扩展错误码映射冲突为 409；失效分析、训练模板引用为 404 | `17313da` |
| A | SQL 分页与 COUNT，批量读取动作和组次，增加稳定排序 | `ab65ab2` |
| B | 新增实际组件交互测试、真实草稿保存和响应丢失后的关联重试集成测试 | `6854144`、`e7c422a`、`68cf28e`、`48690dd`、`1c4459e` |
| C | 统一 CLAUDE/WARP 与真实 AI 配置；更新 env:check；移除无实际消费者的旧模拟逻辑 | `f9a3c93` |
| D | JSON 改称数据导出，明确缺失内容、无 JSON 导入能力及完整目录备份方法 | `5e83416` |

验证结果：

- `npm test`：135 通过，0 失败。
- `npm run lint`：通过；最终 Origin 补丁另通过相关文件 ESLint。
- `npx tsc --noEmit --incremental false`：通过；最终生产构建中的 TypeScript 检查也通过。
- `npm run build`：通过。
- `npm audit --omit=dev`：0 个已报告漏洞。
- `npm run env:check`：正常读取配置，不输出密钥。
- 隔离生产服务：28 项 HTTP 检查通过，包含 10 个页面、有效 GET API、同源写入、跨来源拦截、媒体类型校验、跨历史日期行动生成及删除保护。
- 1000 场训练、每场一个动作和一组、每页一条：分页接口使用 4 次 prepare，原报告为 3001 次。验证关注查询数，不将测试环境耗时作为生产性能承诺。

所有数据库行为测试使用内存或临时数据库；AI 使用模拟响应。生产验证关闭了 AI 密钥，未调用外部 AI，未修改用户现有健康记录。临时生产服务已关闭。

清理临时目录的操作被自动审批审查以 `blocked by policy` 拒绝；仅保留测试数据目录 `C:/Users/PC/AppData/Local/Temp/snapcal-review-verify-bf9b5064-1328-48b6-8283-7c7234317e05`，不在项目或 Git 中。

边界：组件测试使用 jsdom，未覆盖真实手机视觉与手势验收。AI 额度和并发控制保存在当前服务进程内，重启后重置；过敏排除依据已配置名称匹配，不能识别未记录的过敏信息。JSON 仍是数据导出，完整恢复应使用停机目录备份。
